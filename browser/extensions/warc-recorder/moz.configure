# -*- Mode: python; indent-tabs-mode: nil; tab-width: 40 -*-
# vim: set filetype=python:

@depends('warc-recorder')
def warc_recorder(warc_recorder):
    return bool(warc_recorder)

set_config('MOZ_WARC_RECORDER', warc_recorder)
set_define('MOZ_WARC_RECORDER', warc_recorder)
